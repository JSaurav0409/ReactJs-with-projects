import Chai from './chai'

function App() {

  const username = "Saurav Jha"

  return (
    <> {
      // <></> it is called fragments : a Fragment is a component that allows you to return multiple elements without adding an extra node to the DOM. 
      }
    <Chai />
      <h1>React is Library | {username}</h1>
      <p>Test para</p>
    </>
  )
}

export default App

